version: '3.5'
services:
    api:
        container_name: api
        restart: unless-stopped
        build: .
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules/
        ports:
            - '8000:8000'
